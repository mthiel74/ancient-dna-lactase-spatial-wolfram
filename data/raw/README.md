# Raw Data

Downloaded source files belong here. Treat them as immutable after retrieval:

- do not edit raw files in place
- record source URL, retrieval date, checksum, and citation
- set downloaded files read-only after successful checksum recording
- write parser outputs to `data/processed/`

Large raw data files should only be committed if their licenses and file sizes make that appropriate.

