# Tests

Wolfram Language tests using `VerificationTest` belong here.

Use small fixtures for parser, model, inference-summary, and visualization-helper tests. Tests must be non-interactive so they can run in GitHub Actions.

